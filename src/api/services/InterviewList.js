export const getInterviewList = (rawPost) => {
	return console.log(rawPost);
	// return rawPost.map(
	//     ({ id, question, answer}) => {
	//         return {
	//             id: id,
	//             question: question,
	//             answer: answer
	//         }
	//     }
	// )
};
